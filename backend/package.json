{
  "name": "restaurant-reviews-backend",
  "version": "1.0.0",
  "description": "The backend resources for Restaurant Reviews",
  "main": "index.js",
  "scripts": {
    "deploy": "sls deploy -v",
    "postdeploy": "sls invoke -f postdeployment -v"
  },
  "author": "Adrian Hall <photoadrian@outlook.com>",
  "license": "MIT",
  "devDependencies": {
    "aws-amplify-serverless-plugin": "awslabs/aws-amplify-serverless-plugin",
    "serverless": "^1.34.0",
    "serverless-appsync-plugin": "^1.0.9",
    "serverless-cf-vars": "^0.3.0"
  },
  "dependencies": {
    "aws-sdk": "^2.374.0"
  }
}
